import React from 'react';

const Navbar = (props) => {
  style = {
    display: 'flex',

  }
    return(
        <div>
          <h1><a href='/'> Scraper App </a></h1>
        </div>
    );
}

export default Navbar;
